# Frontend configuration (do not commit real secrets)
# Base URL of the notes backend API
# - Local dev: http://localhost:3001
# - Preview: use the deployed backend origin (same as backend /docs host, without the /docs path)
REACT_APP_API_BASE_URL=http://localhost:3001
